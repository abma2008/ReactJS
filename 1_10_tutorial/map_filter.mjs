/**
 * This is an example personally made for remembering how
 * to use map() and filter.
 * For this tutorial here, we will create an array and then
 * initialize it with export
 */

// Creating names array:
 const names = ['Ahmed', 'Mohammed', 'Saeed', 'Faisal', 'Qais'];

/**
 * Note:
 * writing export can be done this way, or:
 * export defaut names 
 * Both can work, but you should stick to one method 
 */

export default names ;
